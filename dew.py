# Функция для перевода температуры из Цельсия в Фаренгейт
def a(c):
    return 1.8 * c + 32

# Заголовок таблицы
print(f"{'Celsius (°C)':<15} {'Fahrenheit (°F)':<15}")

# Цикл для создания таблицы
for c in range(51, 29, -15):  # от 51 до 30 с шагом -15
    f = a(c)
    print(f"{c:<15} {f:<15.2f}")
